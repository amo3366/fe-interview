import './App.css';

/*
 API to get breeds = https://dogapi.dog/api/v2/breeds

 Have to get the list of dog breeds and do the following
 1. Map the data so that we get only the breed name and id
 2. List the data
 3. Add to the list
 4. Remove from the list
 5. Update the list
*/

function App() {
    return (
        <div className="App">
            <div className="main-wrapper">
                <div className={'header'}>
                    Addition deletion and updates
                </div>
            </div>
        </div>
    );
}

export default App;
